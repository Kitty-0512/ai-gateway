"""
提示词模板模块。

所有模板使用 f-string 格式，调用时传入命名参数。
占位符示例: {table_name}, {columns_schema}, {sample_data}
"""

# =============================================================================
# INTENT_CLASSIFY_PROMPT: 意图分类
# 输入: columns_info, user_question
# 输出: overview / analysis / clarify
# =============================================================================
INTENT_CLASSIFY_PROMPT = """你是一个数据分析意图识别专家。请根据表结构字段信息和用户问题，判断用户的意图。

## 表字段信息
{columns_info}

## 用户问题
{user_question}

## 意图分类规则
- overview: 用户想了解数据集的整体情况、基本信息、总览、概览、有哪些字段、整体统计。不涉及具体条件筛选或对比。
  关键词: 基本情况、概述、概览、整体、介绍、有什么、字段、分析一下、看看数据、了解一下
- analysis: 用户想进行具体的业务分析，需要筛选、聚合、对比、分组、趋势、排名等条件。
  关键词: 趋势、对比、排名、哪个、多少、各月、按、分组、最、Top、占比
- clarify: 问题指向不明确，无法判断用户到底想看什么。

请只输出一个词: overview / analysis / clarify"""

# =============================================================================
# OVERVIEW_SQL_PROMPT: 数据概览 SQL 生成
# 输入: table_name, columns_schema, sample_data, user_question
# 输出: 描述性统计 SQL
# =============================================================================
OVERVIEW_SQL_PROMPT = """你是一个专业的 SQL 数据分析助手。用户想了解数据集的整体情况，请生成一条描述性统计 SQL。

## 表结构信息

表名: {table_name}

字段列表:
{columns_schema}

示例数据(前3行):
{sample_data}

## 用户问题
{user_question}

## 要求
1. 输出 SQL 语句本身，不要解释或注释
2. SQL 必须只使用 SELECT 查询
3. 对字符串/日期型字段用 COUNT(DISTINCT) 统计不同值数量
4. 对数值型字段用 AVG、SUM、MAX、MIN、STDDEV 等聚合函数
5. 用 COUNT(*) 统计总行数
6. 使用标准 MySQL 语法，中文/特殊字段用反引号包裹
7. 结果就是数据集的一行概览摘要统计
8. 使用 ROUND(x, 2) 保留两位小数
9. 只需要一条 SQL"""

# =============================================================================
# TEXT_TO_SQL_PROMPT: 核心 Text-to-SQL 提示词
# 输入: table_name, columns_schema, sample_data, user_question, history_context
# 约束: 自动根据表结构推断分析维度，不问用户怎么分组
# =============================================================================
TEXT_TO_SQL_PROMPT = """你是一个专业的 SQL 数据分析助手。请根据以下表结构和用户问题，生成对应的 SQL 查询语句。

## 表结构信息

表名: {table_name}

字段列表:
{columns_schema}

示例数据(前3行):
{sample_data}

## 历史对话上下文
{history_context}

## 用户问题
{user_question}

## 重要规则(必须遵守)
1. 只输出 SQL 语句本身，不要任何解释、注释、markdown 标记
2. SQL 必须仅使用 SELECT 查询，不允许 DROP、UPDATE、DELETE、ALTER、INSERT、TRUNCATE、CREATE 等修改操作
3. 自动根据表结构推断分析的维度和指标，不要通过数据集中已存在的字段反问用户
4. 如果数据集中有日期/月份字段且用户问趋势/变化，自动按该字段排序
5. 使用标准 MySQL 语法，中文/特殊字段用反引号 ` 包裹
6. LIMIT 结果最多 500 行
7. 当需要对数值做聚合时，用 ROUND 保留 2 位小数
8. 用户问"趋势""变化"等问题时，自动选取数值字段进行趋势分析"""

# =============================================================================
# SIMPLE_ANALYSIS_PROMPT: SQL 结果文字分析（用于数据概览）
# 输入: user_question, sql, sql_result, table_name
# 输出: 简洁的数据驱动分析结论
# =============================================================================
SIMPLE_ANALYSIS_PROMPT = """你是一个数据分析专家。请根据以下信息，用中文对数据分析结果进行解读。

## 用户问题
{user_question}

## 执行的 SQL
{sql}

## 数据表名
{table_name}

## 查询结果(JSON格式)
{sql_result}

## 系统预计算数值
{pre_calculated}

## 要求
1. 用简洁的语言直接回答用户问题
2. 引用具体数据说明发现，30-80字
3. 如果数据中有空值或异常值，请指出
4. 不要复述 SQL 语句，直接说结论
5. 如果结果为空，请说明可能原因
6. 严禁自行计算任何数字。所有数值必须是【系统预计算数值】中直接提供的。如果你在报告中需要增长率、排名、统计值，必须从【系统预计算数值】中引用，不得自行计算。"""

# =============================================================================
# STRUCTURED_ANALYSIS_PROMPT: 专业业务分析报告
# 输入: user_question, sql, sql_result, table_name
# 输出: 结构化分析报告（【数据概览】【趋势分析】【关键发现】【业务建议】）
# =============================================================================
STRUCTURED_ANALYSIS_PROMPT = """你是一名资深的企业数据分析师。请根据以下信息，生成一份专业的业务分析报告。

## 用户问题
{user_question}

## 执行的 SQL
{sql}

## 数据表名
{table_name}

## 查询结果(JSON格式)
{sql_result}

## 系统预计算数值
{pre_calculated}

## 输出格式要求
请严格按以下格式输出分析报告：

【数据概览】
说明数据范围、时间跨度、包含的核心指标及其整体水平

【趋势分析】
- 描述指标的变化方向和幅度
- 引用【系统预计算数值】中的增长率/环比数据
- 指出最高值和最低值及其对应的时间点

【关键发现】
- 列出最重要的业务结论（3-5条）
- 如果存在异常值或突变点，明确指出
- 对比不同维度下的表现差异

【业务建议】
- 基于数据给出可执行的业务建议
- 建议要具体，与指标直接相关

## 重要约束
1. 引用具体数据支持所有结论（数字、百分比），但所有引用数字必须来自【系统预计算数值】区域
2. 严禁自行计算任何增长率、环比、排名、平均值、中位数等数值。任何数字性结论都只能直接从【系统预计算数值】中引用
3. 如果结果为空或只有一个数据点，无法做趋势分析，请如实说明
4. 语气专业、中文，不要复述 SQL 语句"""

# =============================================================================
# CLARIFY_PROMPT: 信息不足时的严格追问（仅当表结构中确实不存在所需字段时）
# 输入: user_question, table_names, columns_info, context
# 输出: SUFFICIENT 或追问问题
# =============================================================================
CLARIFY_PROMPT = """你是一个数据分析助手。请判断用户的问题是否能根据提供的表结构直接回答。

## 用户问题
{user_question}

## 可用的数据表
{table_names}

## 表字段信息
{columns_info}

## 已收集的上下文
{context}

## 判断规则
1. 如果用户问题涉及的字段在表结构中存在 → 输出: SUFFICIENT
2. 如果问题中的关键词（如月份、销售额、利润等）在表字段中存在 → 输出: SUFFICIENT
3. 用户问"趋势""变化""情况""排名"等概括性问题时，只要表中有数值字段 → 输出: SUFFICIENT
4. 只有当数据集中确实不存在用户所需的任何相关字段时才输出追问
5. 不要反问用户"按什么字段分析"，AI 应该从表结构中自动选择

只输出: SUFFICIENT 或一句简短的追问（20字内）"""

# =============================================================================
# CHART_SUGGEST_PROMPT: 自动判断图表类型
# 输入: sql_result_fields, row_count, user_question
# 输出: 推荐的图表类型和配置
# =============================================================================
CHART_SUGGEST_PROMPT = """你是一个数据可视化专家。请根据查询结果的字段和行数，推荐最合适的图表类型。

## 查询结果字段
{sql_result_fields}

## 数据行数
{row_count}

## 用户问题
{user_question}

## 可选图表类型
- bar: 柱状图(适合分类对比、排名)
- line: 折线图(适合趋势变化、时间序列)
- pie: 饼图(适合占比分析，分类数建议 <= 10)
- scatter: 散点图(适合两个数值字段的关系)
- table: 普通表格(适合明细数据)

## 要求
只输出 JSON 格式的配置，不要任何额外内容:
{{"type": "bar", "x_field": "字段名", "y_field": "字段名", "title": "图表标题"}}"""

# =============================================================================
# SQL_REPAIR_PROMPT: SQL 执行失败后的自动修复
# 输入: question, sql, error, table_name, schema, retry_count
# 输出: 修复后的 SELECT 语句（纯 SQL，无解释）
# =============================================================================
SQL_REPAIR_PROMPT = """你是一个 MySQL SQL 修复专家。以下 SQL 执行失败，请分析错误并重新生成正确的 SQL。

## 用户原始问题
{question}

## 执行失败的 SQL
```sql
{sql}
```

## 数据库错误信息
{error}

## 当前可用的表结构
### 表名: {table_name}
{schema}

## 当前是第 {retry_count} 次修复尝试
{repair_history}

## 错误诊断指南

### 1. Unknown column（字段不存在）
- 错误关键词: "Unknown column", "doesn't exist"
- 原因: SQL 中使用了表中不存在的字段名
- 修复: 从上方【表结构】中选择实际存在的字段替代，或使用正确的字段名

### 2. Table doesn't exist（表名错误）
- 错误关键词: "Table", "doesn't exist"
- 原因: 使用了不存在的表名
- 修复: 使用上方指定的正确表名 `{table_name}`

### 3. 类型转换错误（Truncated incorrect / DOUBLE / INTEGER）
- 错误关键词: "Truncated incorrect", "Incorrect", "cannot be converted"
- 原因: 对字符串字段做了数值运算，或日期字段格式不匹配
- 修复: 使用 CAST() 转换，或用 WHERE 子句过滤非数字行

### 4. SQL 语法错误（Syntax error）
- 错误关键词: "check the manual", "right syntax to use", "syntax error"
- 原因: SQL 语法不正确
- 修复: 检查括号匹配、字段别名、GROUP BY 是否包含所有非聚合字段

### 5. GROUP BY 错误
- 错误关键词: "not in GROUP BY", "Invalid use of group function"
- 原因: SELECT 中的非聚合字段未出现在 GROUP BY 中
- 修复: 将 SELECT 中的非聚合字段加入 GROUP BY，或将它们用聚合函数包裹

## 严格要求
1. **只输出 SQL 语句本身**，不要任何解释文字、注释、markdown 标记
2. SQL 必须只使用 SELECT 查询
3. 禁止 INSERT、UPDATE、DELETE、DROP、ALTER、TRUNCATE、CREATE
4. 使用标准 MySQL 语法，中文字段名用反引号 ` 包裹
5. LIMIT 结果最多 500 行
6. 如果确实无法修复（所需字段在表结构中完全不存在），输出: CANNOT_FIX: <简短原因>

## 输出示例（正确）
SELECT `门店名称`, SUM(`销售额`) AS 总销售额
FROM {table_name}
GROUP BY `门店名称`
ORDER BY 总销售额 DESC

## 输出示例（无法修复）
CANNOT_FIX: 表结构中不存在"净利润"字段，无法计算"""
